name: Arduino Lib

on:
    pull_request:
        branches:
            - master
    push:
        branches:
            - master

jobs:
    Arduino-Checks:
        uses: sensirion/.github/.github/workflows/upt.arduino.check.yml@update-6
        with:
            expect-arduino-examples: true
            lint-lib-manager-check: update
